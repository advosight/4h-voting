---
phase: 01-finish-fit-show
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/FitShowScoringForm.tsx
  - src/components/__tests__/FitShowScoringForm.optimisticQueue.test.tsx
  - src/components/FitShowScoreLeaderboard.tsx
autonomous: true
requirements: [D-02, D-03, D-04]

estimate:
  tokens: 39000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Clicking submit shows an immediate 'Score submitted!' confirmation with a spinner before the server has responded (D-04)."
    - "If a submission fails (offline or server error), the judge's answers are not lost — they are retried automatically once connectivity returns (D-03)."
    - "The leaderboard's Health Examination progress bar shows the correct 24-point maximum, matching the corrected label from plan 01-01."
  artifacts:
    - "src/components/FitShowScoringForm.tsx persists a pending submission to localStorage before calling the mutation and clears it on success"
    - "src/components/FitShowScoringForm.tsx retries a queued submission on the browser 'online' event"
  key_links:
    - "The queued localStorage entry must be scoped to catId+judgeId so a stale entry from a different cat/judge is never replayed onto the wrong score"
    - "A queued retry must be skipped (not blindly replayed) if the server's current score for this cat+judge is now newer than the queued timestamp, to avoid clobbering a save made from another device"
---

<objective>
Layer real-time submission UX onto the completed scoring form from plan 01-01: optimistic
"submitted" feedback shown before the network call resolves (D-04), and a local-first
queue that survives a dropped connection and auto-syncs on reconnect (D-03). Also correct
the leaderboard's stale Health Examination max label so it agrees with plan 01-01's fix.

Purpose: D-03 and D-04 are explicit, locked UX decisions distinct from D-01's live-update
wiring (done in plan 01-01) and D-02's server-side conflict policy (done in plan 01-02).
Without this plan, a judge who submits on a flaky venue Wi-Fi connection can silently lose
a completed score.

Output: FitShowScoringForm shows immediate optimistic feedback and queues/retries failed
submissions; FitShowScoreLeaderboard's category breakdown shows the correct 24-point
Health Examination maximum.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-finish-fit-show/01-CONTEXT.md
@src/components/FitShowScoringForm.tsx
@src/components/FitShowNetworkErrorHandler.tsx
@src/components/FitShowScoreLeaderboard.tsx
@index.html
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Optimistic submit feedback and offline queue with reconnect sync</name>
  <files>src/components/FitShowScoringForm.tsx, src/components/__tests__/FitShowScoringForm.optimisticQueue.test.tsx</files>
  <read_first>
    src/components/FitShowScoringForm.tsx (as rebuilt by plan 01-01 — read the current
    state on disk, not the pre-01-01 version, especially `handleSubmit`/`saveScore`),
    src/components/FitShowNetworkErrorHandler.tsx (existing, currently-unused component
    with exactly the `error`/`onRetry`/`maxRetries`/`autoRetry` props this task needs for
    surfacing a failed submission with retry — wire it in rather than building a new
    bespoke error UI), index.html (existing global `online`/`offline` body-class handling
    already wired via `window.addEventListener('online'/'offline', ...)` — reuse the same
    event, don't add a second listener mechanism)
  </read_first>
  <behavior>
    - Test: clicking submit immediately renders a "Score submitted!" (or equivalent
      optimistic) message and a spinner, before the mocked GraphQL promise resolves.
    - Test: before the mutation call is made, the pending submission payload is written to
      `localStorage` under a key scoped to the current `catId` and `judgeId`
      (e.g. `fitshow-queue-${catId}-${judgeId}`).
    - Test: on mutation success, the localStorage entry for that key is removed.
    - Test: on mutation failure, the localStorage entry remains, `FitShowNetworkErrorHandler`
      is rendered with the error, and dispatching a `window` `online` event triggers a
      retry of the same queued payload without the judge re-entering anything.
    - Test: on mount, if a queued entry already exists in localStorage for this
      `catId`+`judgeId` (left over from a previous session), the form seeds its state from
      it rather than starting fresh.
  </behavior>
  <action>
    In `handleSubmit`, before calling `client.graphql` for create/update: (1) set a new
    `submitStatus` state to an optimistic "submitted" value and render it immediately
    (e.g. "Score submitted!" text plus a spinner, replacing/augmenting the existing
    `isSubmitting` disabled-button treatment — keep the button disabled during this
    window); (2) write the current `scoreData` plus `catId`, `judgeId`, and a timestamp to
    `localStorage.setItem('fitshow-queue-' + catId + '-' + judgeId, JSON.stringify(...))`.
    On the mutation's success path, `localStorage.removeItem` that same key and update
    `submitStatus` to a success state (e.g. replacing the spinner with a checkmark) before
    calling `onScoreSubmitted`. On failure, leave the queued entry in place, capture the
    error into a `NetworkError`-shaped object (message, code, operation: 'submit score',
    timestamp), and render `<FitShowNetworkErrorHandler error={...} onRetry={() =>
    saveScore(false)} autoRetry maxRetries={3} />` below the form.

    Add a `useEffect` on mount: read `localStorage.getItem('fitshow-queue-' + catId + '-'
    + judgeId)`; if present, only auto-replay it if `navigator.onLine` is true AND (there
    is no `existingScore`, OR the queued entry's timestamp is not older than
    `existingScore.updatedAt`) — this guards against blindly overwriting a newer save made
    from a different device/tab with a stale queued one. If the queued entry is older than
    the current server score, discard it (localStorage.removeItem) instead of replaying it,
    and log why via console.warn. If it is safe to replay, seed `scoreData` from it and
    call `saveScore(false)` once mounted. Also add a `window.addEventListener('online',
    retryQueued)` (reusing the existing global online/offline mechanism already present in
    index.html, not a second detection scheme) that re-attempts a still-pending queued
    submission, removing the listener on unmount.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/FitShowScoringForm.optimisticQueue.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - New test file's 5 cases pass.
    - `npx vitest run src/components/__tests__/FitShowScoringForm.test.tsx` still passes
      16/16 (this task must not regress plan 01-01's target suite).
    - FitShowScoringForm.tsx imports and renders `FitShowNetworkErrorHandler`.
    - FitShowScoringForm.tsx calls `localStorage.setItem`/`removeItem` with a key
      containing both `catId` and `judgeId`.
  </acceptance_criteria>
  <done>A judge who submits while offline sees immediate optimistic confirmation, and
  their answers are neither lost nor silently duplicated — the same submission
  auto-completes once the connection returns.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the leaderboard's Health Examination maximum</name>
  <files>src/components/FitShowScoreLeaderboard.tsx</files>
  <read_first>src/components/FitShowScoreLeaderboard.tsx (getCategoryBreakdown function)</read_first>
  <action>
    In `getCategoryBreakdown`, change the Health Exam entry's `max: 21` to `max: 24` — the
    nine health fields' actual maximums (3+2+2+2+2+2+2+3+6) sum to 24, matching the fix
    already applied to HealthExaminationScoring.tsx in plan 01-01. Leave every other
    category's max (Appearance 20, Handling 14, Demonstration 16, Grooming 14, Knowledge
    12) unchanged — they were already correct.
  </action>
  <verify>
    <automated>grep -n "name: 'Health Exam'" -A1 src/components/FitShowScoreLeaderboard.tsx | grep -q "max: 24"</automated>
  </verify>
  <acceptance_criteria>
    - `getCategoryBreakdown`'s Health Exam entry reads `max: 24`.
    - The six category maximums in `getCategoryBreakdown` sum to 100
      (20+14+16+24+14+12).
  </acceptance_criteria>
  <done>The leaderboard's Health Examination progress bar denominator matches the form's
  actual 24-point category maximum.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Judge browser localStorage | Client-controlled persistent storage on a possibly-shared device |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | Queued score payload in localStorage | low | accept | Score data (numeric ratings, short comments) is not highly sensitive PII and is already visible on-screen; the queue entry is removed immediately on successful sync, minimizing residency |
| T-03-02 | Tampering (stale replay) | Auto-replay of a leftover queued entry on mount | medium | mitigate | Replay is skipped (and the stale entry discarded) whenever the queued timestamp is older than the current server score's `updatedAt`, preventing an old local draft from clobbering a newer save made elsewhere |
</threat_model>

<verification>
1. `npx vitest run src/components/__tests__/FitShowScoringForm.optimisticQueue.test.tsx` — passes.
2. `npx vitest run src/components/__tests__/FitShowScoringForm.test.tsx` — still 16/16.
3. `grep -n "max: 24" src/components/FitShowScoreLeaderboard.tsx` — present.
</verification>

<success_criteria>
- Submitting a score gives instant feedback and survives a dropped connection.
- Leaderboard category maximums are internally consistent and sum to 100.
</success_criteria>

## Artifacts this phase produces (this plan)
- `src/components/FitShowScoringForm.tsx` — optimistic submit state, localStorage queue,
  online-event retry, `FitShowNetworkErrorHandler` wiring.
- `src/components/__tests__/FitShowScoringForm.optimisticQueue.test.tsx` (new)
- `src/components/FitShowScoreLeaderboard.tsx` — corrected Health Exam `max: 24`.

<output>
Create `.planning/phases/01-finish-fit-show/01-03-SUMMARY.md` when done
</output>
