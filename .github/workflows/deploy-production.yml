name: Deploy to Production

on:
  push:
    branches: [main]

# Prevent overlapping production deploys if multiple pushes land close together.
concurrency:
  group: deploy-production
  cancel-in-progress: false

permissions:
  id-token: write   # required for AWS OIDC
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          # Node 20 rejects --localstorage-file in NODE_OPTIONS (used by every
          # npm script in this repo, e.g. `build`/`test`); Node 22+ allows it.
          node-version: '22'
          cache: npm

      - name: Install frontend dependencies
        run: npm ci

      - name: Build frontend
        # `vite build` (via esbuild) transpiles without type-checking -- same as the
        # prior CRA/webpack bundle output, which doesn't fail on type errors either.
        # There's a real amount of pre-existing type debt in src/ (run `npm run
        # typecheck` to see it) that was never a hard gate; not enforcing it here
        # preserves that. (tsconfig.app.json covers src/ for the IDE and vitest; the
        # root tsconfig.json only covers infrastructure/**.)
        #
        # The production environment's vars are still named REACT_APP_* (from the
        # pre-Vite setup); map them to the VITE_* names the app now reads via
        # import.meta.env. Nothing previously wired these into the build at all, so
        # production has been silently running on the hardcoded fallback values in
        # src/index.tsx until now.
        env:
          VITE_USER_POOL_ID: ${{ vars.REACT_APP_USER_POOL_ID }}
          VITE_USER_POOL_CLIENT_ID: ${{ vars.REACT_APP_USER_POOL_CLIENT_ID }}
          VITE_REGION: ${{ vars.REACT_APP_REGION }}
          VITE_GRAPHQL_ENDPOINT: ${{ vars.REACT_APP_GRAPHQL_ENDPOINT }}
          VITE_VOTING_API_ENDPOINT: ${{ vars.REACT_APP_VOTING_API_ENDPOINT }}
        run: npm run build

      - name: Install infrastructure dependencies
        working-directory: infrastructure
        run: npm ci

      - name: Typecheck infrastructure
        working-directory: infrastructure
        run: npx tsc --noEmit -p .

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars.AWS_DEPLOY_ROLE_ARN }}
          aws-region: us-west-2

      - name: Deploy (CDK)
        working-directory: infrastructure
        run: npx cdk deploy CatVotingStack -c stage=production --require-approval never
